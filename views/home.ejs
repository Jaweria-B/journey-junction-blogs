<%- include('partials/header'); -%>

<h1>Journey into the Realm of Words 📝</h1>

<p> <%= startingContent %></p>

<%  posts.forEach(function(post){ %>

    <div class="post">
        <h1><%= post.title %></h1>
        <p><%= post.content.substring(0, 100) + "..." %><a href="/posts/<%= post._id %>">Read More</a></p>
        
    </div>
    
    <% }) %>

<%- include('partials/footer'); -%>

